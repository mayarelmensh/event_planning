specVersion: "v1beta"
serviceId: "event_planning"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "event_planning-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
